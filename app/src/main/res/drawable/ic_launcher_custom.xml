<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- El fondo del icono (puedes usar un color o una imagen) -->
    <background android:drawable="@color/white"/>

    <!-- El logo propiamente dicho -->
    <foreground>
        <inset
            android:drawable="@drawable/ic_app"
            android:inset="10%"/> <!-- Reduce este porcentaje (ej. 5% o 0%) para que el logo se vea más GRANDE -->
    </foreground>
</adaptive-icon>